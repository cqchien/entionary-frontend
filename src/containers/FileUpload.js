import React from "react";
import UploadButton from "../components/UploadButton";

const FileUpload = ({ title, name }) => {
  return <UploadButton title={title} name={name} />;
};

export default FileUpload;
